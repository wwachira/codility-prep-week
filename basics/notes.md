# Sting topics

-   Creating strings
-   Template literals
-   Length of a string
-   Accessing Strings
-   charAt() method
-   Looping through a string
-   String methods
    -   replace()
    -   replaceAll()
    -   indexOf()
    -   lastIndexOf()
    -   slice()
    -   toUpperCase()
    -   toLowerCase()
    -   includes()
-   Splitting and joining strings
    -   split()
    -   join()
